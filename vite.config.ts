// Shared Vite/TanStack config already includes the standard build plugins and path aliases.
// Do not duplicate React, Tailwind, tsConfigPaths, or Cloudflare build plugins here.
// You can pass extra Vite config via defineConfig({ vite: { ... } }) if needed.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
